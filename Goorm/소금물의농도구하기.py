# https://level.goorm.io/exam/194193/%EC%86%8C%EA%B8%88%EB%AC%BC%EC%9D%98-%EB%86%8D%EB%8F%84-%EA%B5%AC%ED%95%98%EA%B8%B0/quiz/1

# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean

# 7% 소금물
# 소수점 2번째 자리까지 출력

N, M = map(int,input().split())

base_salt = N * 0.07
after_salt = ((base_salt) / (N+M)) * 100

print(f"{after_salt:.2f}")

"""
=== 구름 194193번 코드 리뷰 (2차: 실패 원인 분석) ===

[3가지 케이스 실패 원인 분석]
사용자님이 말씀하신 실패는 다음 두 가지 원인 중 하나일 가능성이 매우 높습니다.

1. **소수점 처리 방식의 차이 (버림 vs 반올림)**
   - 문제에서 "소수점 2번째 자리까지 출력"하라고 할 때, `printf`나 `:.2f`는 기본적으로 **반올림**을 수행합니다.
   - 하지만 많은 코딩 테스트 문제(특히 구름)에서는 **버림(절삭)**을 요구하는 경우가 많습니다.
   - 예: `3.456` → 반올림하면 `3.46`, 버림하면 `3.45`
   - 해결책: `math.floor`를 사용하거나 정수 연산으로 버림 처리를 해야 합니다.

2. **부동소수점 정밀도 오차**
   - `0.07`은 컴퓨터가 이진수로 정확하게 표현할 수 없는 수입니다. (`0.069999...` 형태)
   - `base_salt = N * 0.07` 계산 과정에서 미세한 오차가 발생하고, 이것이 `* 100`을 하면서 커질 수 있습니다.

[개선 힌트: 정수 연산 활용]
소수점 연산을 아예 하지 않고 정수 연산만으로 처리하면 오차와 버림 문제를 동시에 해결할 수 있습니다.

농도 공식: `(소금 / 소금물) * 100`
이것을 `0.07` 없이 표현하면: `(7 * N / (N + M))` 입니다.
여기서 소수점 2자리까지 **버림**하여 출력하려면:

```python
# 힌트 로직 (정수만 사용)
# 1. 100을 추가로 곱해서 소수점 2자리를 정수부로 올립니다.
# 공식: (7 * N * 100) / (N + M) 
# 이것을 정수 나눗셈(//) 하면 자동으로 소수점 아래가 버려집니다.
val = (7 * N * 100) // (N + M)

# 2. 다시 실수로 변환하여 출력
print(f"{val / 100:.2f}")
```

[수정 제안]
1. `0.07`을 곱하는 대신 `7`을 곱하는 방식으로 변경해보세요.
2. `round`나 `format`의 자동 반올림 대신, 정수 나눗셈(`//`)을 이용한 버림 처리를 시도해보세요.
"""


# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean

# 7% 소금물
# 소수점 2번째 자리까지 출력

N, M = map(int,input().split())

after_salt = ((N * 7 * 100) // (N+M))

print(f"{after_salt/100:.2f}")