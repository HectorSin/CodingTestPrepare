# 2s, 128mb
# 문제 분석: 잃어버린 괄호 (백준 1541)
# https://www.acmicpc.net/problem/1541
# 목표: 괄호를 적절히 쳐서 식의 값을 최소로 만들기
# 입력: 0~9, +, - 로 이루어진 식 (길이 최대 50)
# 힌트: 값을 가장 작게 만드려면 '빼기'를 가장 크게 만들어야 합니다.



# 💡 Level 1 힌트:
# 식의 값을 최소화하려면, 마이너스(-) 뒤에 나오는 묶음을 최대한 크게 만들어서 빼주면 됩니다.
# 예: 55 - 50 + 40 -> 55 - (50 + 40) = -35
# 즉, 처음 - 가 나오면 그 뒤의 모든 숫자는 뺄셈으로 처리할 수 있는지 생각해보세요.

# equation = input()


# 현재 접근: 입력을 한 글자씩 파싱하여 리스트로 만들고 있습니다.
# 이 방법도 좋지만, 파이썬의 문자열 메서드를 활용하면 더 간단하게 처리할 수 있습니다.

# 💡 Level 2 힌트 (구현):
# '-'를 기준으로 식을 쪼개면 어떻게 될까요?
# 예: "55-50+40" -> ["55", "50+40"]
# 첫 번째 그룹("55")은 더하고, 나머지 그룹("50+40") 내의 숫자들은 모두 더해서 빼주면 됩니다.

# eq_list = []
# target = ""

# for i in equation:
#     if i == "+" or i == "-":
#         eq_list.append(target)
#         eq_list.append(i)
#         target = ""
#     else:
#         target += i
# eq_list.append(target)

# 현재 코드 이후에는 eq_list를 순회하며 계산하는 로직이 필요합니다.
# 만약 Level 2 힌트의 접근법(split 사용)을 쓴다면 반복문이 훨씬 단순해질 수 있습니다.


equation = input()
eq_list = []
target = ""

for i in equation:
    if i == "-":
        eq_list.append(target)
        target = ""
    else:
        target+=i
eq_list.append(target)
# print(eq_list)

seq_list = []
seq = 0
for j in eq_list:
    if "+" in j:
        sum_seq = sum(map(int,j.split("+")))
    else:
        sum_seq = int(j)
    seq_list.append(sum_seq)
    seq += sum_seq

if len(seq_list) >= 2:
    # 올바른 접근: 첫 번째 값에서 나머지 값들을 모두 뺍니다.
    answer = seq_list[0]
    for num in seq_list[1:]:
        answer -= num
    print(answer)

else:
    print(seq_list[0])


    