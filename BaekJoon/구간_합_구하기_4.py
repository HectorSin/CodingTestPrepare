# 1s, 256mb
# 문제 분석: 주어진 구간 (i, j)의 합을 구하는 문제입니다. 
# M이 최대 10만 번이므로, 매번 더하면 O(N*M)이 되어 시간 초과가 발생합니다.
# 현재 접근: 필요할 때마다 부분적으로 합을 계산하는 방식(Memoization 유사)을 사용하고 계시네요.
# 이 방식도 O(N+M)으로 동작할 수 있지만, 일반적으로는 미리 모든 합을 구해두는 '누적 합(Prefix Sum)' 배열을 만드는 것이 구현이 더 간단하고 빠릅니다.


"""
import sys
input = sys.stdin.readline

# 💡 힌트 1: 입력값 N의 크기가 정해져 있으니, 미리 크기에 맞는 배열을 준비하면 좋습니다.
# dp_table = [0] * (N + 1) 처럼 N을 입력받은 후에 만드는 것이 메모리 관리에 더 좋습니다.
dp_table = [0] * 100001

N,M = map(int,input().split())

N_list = list(map(int,input().split()))

# 💡 힌트 2: 쿼리(질문)를 받기 전에, 수열의 앞에서부터 누적된 합을 미리 다 계산해두세요.
# for k in range(N):
#     dp_table[k+1] = dp_table[k] + N_list[k]
# 이렇게 하면 나중에 for문이나 if문 없이 바로 dp_table[j] - dp_table[i-1]로 답을 구할 수 있습니다.

for _ in range(M):
    i, j = map(int,input().split())

    # 현재 코드는 쿼리마다 계산 여부를 확인하고 있습니다.
    # 미리 다 계산해두었다면 이 if-for 로직이 전부 필요 없어지고, 출력 한 줄만 남게 됩니다.
    if dp_table[j] == 0:
        for a in range(1,j+1):
            if dp_table[a] == 0:
                dp_table[a] = dp_table[a-1] + N_list[a-1]

    print(dp_table[j] - dp_table[i-1])
"""

import sys
input = sys.stdin.readline

N,M = map(int,input().split())

dp_table = [0] * (N+1)

N_list = list(map(int,input().split()))

for k in range(N+1):
    dp_table[k] = dp_table[k-1] + N_list[k-1]

for _ in range(M):
    i, j = map(int,input().split())

    print(dp_table[j] - dp_table[i-1])

# 위 방식으로 교체후 시간 복잡도 개선 348ms -> 240ms